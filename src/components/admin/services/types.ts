
export interface ServiceFormData {
  title: string;
  description: string;
  icon: string;
  is_active: boolean;
  order_index: number;
}

export interface IconOption {
  value: string;
  label: string;
  icon: any;
}
